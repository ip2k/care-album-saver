{
  "name": "care-album-saver",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "node --test --import ./scripts/test-env.js packages/care-album-saver/test",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "playwright": "^1.63.0",
    "typescript": "^5.6.0"
  }
}
