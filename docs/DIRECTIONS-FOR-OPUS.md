# Directions for Opus — from Fable's review of 2026-09-22

This is the work that follows from the answers in [QUESTIONS-FOR-FABLE.md](QUESTIONS-FOR-FABLE.md).
Read that file first; this one only says what to do, in what order, and how to know it is
done. The review was made against `main` at fcdd99e and the four branches `ux/shell`,
`ux/cookie-help`, `ux/native-dialogs`, `ux/scheduling`. **While it was being written, an
Opus session merged all four into `main` as they stood (3cf4581, d8e44e4, 68db0f6,
362ecb4).** So Part 1 below is no longer a merge plan: every "fix on the branch first"
item is now a fix to make on `main`, on a branch of its own, and the branch worktrees
under `.claude/worktrees/wf_37df831c-ca0-*` can all be removed (`git worktree remove`; 8
is locked by a dead agent, `git worktree unlock` first). The server.ts conflict between
native-dialogs and scheduling was resolved by whoever merged them; check that only one
`openFolder` survived (1.4) — the working tree had uncommitted edits to `web/server.ts` and
`test/native-dialogs.test.js` when this was written, which may be that resolution.

Test counts on 2026-09-22: main 122; shell 125; cookie-help 133; native-dialogs 136;
scheduling 146. All green. Nothing was installed on the developer Mac by any of those runs
(checked `~/Library/LaunchAgents` and `crontab -l`).

House rules that apply throughout: a branch per stream, `--no-ff` merge into `main`, no
remote exists so nothing is pushed; two env vars before any script touches config or
photos (`scripts/test-env.js`); no real personal data anywhere; zero runtime dependencies
— `node -e`, never `rimraf`; no Python.

## Part 1 — Fixes the branch reviews found (now all on `main`)

None of these touch the files Part 2 changes, so the order between the parts is free.

### 1.1 From `ux/shell` — two changes
- **Revert the rename.** "Care Album Saver" is a third name for a product that is
  `brightwheel-archive` in the package, the CLI, the config directory, the README and the
  GUIDE. On the branch, make `<title>` and `<h1>` in `src/web/page.ts` both `Brightwheel
  Archive` (its own test wants them equal) and restore the one-line lede under the heading
  ("Saves your child's photos from your own Brightwheel account onto this computer, sorted
  into a folder for each week.") with its `.lede` CSS and the `.lede` selector in
  `scripts/screenshots.js`'s annotate list. If the owner wants distance from the
  Brightwheel mark, that is a recorded decision that renames everything including the
  config directory (with a migration), not a page title. Ask; do not do both.
- **Pin the one outward link exactly.** In `test/page-identity.test.js` and
  `test/integration.test.js` assert `href="https://schools.mybrightwheel.com/"` with the
  closing quote, and assert the `rel` set contains both `noopener` and `noreferrer`
  regardless of order. Keep the link and `target=_blank`; they are right.

### 1.2 From `ux/cookie-help` — regenerate once; one doc line
- The five `docs/images/*.png` were regenerated by both branches; whichever set the merge
  kept is stale for the other. Run `pnpm build && node scripts/screenshots.js && node
  scripts/cookie-help-images.js` once after 1.1 and commit the images.
- Add the link from `docs/GUIDE.md` step 2 to `docs/COOKIE.md` (the branch's commit says
  it belongs to another lane). Make the GUIDE's `schools.mybrightwheel.com` a link too, for
  the reason the page gives.
- Verified: the invented cookie value trips none of the three gitleaks rules; the page
  stays under the 150 KB cap; browser detection is UA-sniffing that only reorders sections.
  Not verified by anyone: that the drawings match the current DevTools layouts in all
  three browsers. Ask the owner to eyeball each once against a real browser before the
  images ship in a README.

### 1.3 From `ux/native-dialogs` — one line of page copy
Reviewed `src/native.ts` and the two routes: fixed AppleScript/PowerShell literals, argv
arrays everywhere, absolute-path check with a leading-dash refusal, one dialog at a time
with a 10-minute timeout, the picked path routed through the same `checkArchiveDir` as a
typed one. Tests stub the spawner. One UX note to add to the page copy: on macOS the
chooser opens behind the browser window with no focus change; say "look for the window
that opened". No TCC prompt is expected for `choose folder`.

### 1.4 From `ux/scheduling` — four things, and one check
- **Check the merge resolution in `src/web/server.ts`:** there must be one `/api/open-folder`,
  backed by `native.ts`'s `openFolder`; delete `maintenance.ts`'s `openFolder` and its route
  if both survived.
- **Tell the parent when a scheduled run fails.** After `run --scheduled` records
  `ok: false`, emit a desktop notification with fixed text naming nobody ("The daily photo
  run did not work. Open the setup assistant to see why."): `osascript -e 'display
  notification …'` on macOS, `notify-send` on Linux if present, nothing on Windows for
  now. Through `execFile` with an argument array, and never with a message built from the
  error text.
- **Detect a job that exists but never runs.** Add `installedAt` to `ScheduleRecord`; in
  `status()`, when the job is registered but `lastRun` is null or older than the last
  scheduled occurrence by more than a day, say so in the summary ("set up on the 3rd; has
  not run since the 9th"). This is the case where `nodePath` was an nvm/volta path that
  changed on upgrade or `cliPath` was inside an npx/dlx cache: at install time, warn when
  `cliPath` contains `_npx`, `dlx` or `.pnpm-store`, or `nodePath` contains `.nvm`,
  `.volta` or `fnm`, and say the job will stop working when that version is removed.
- **Duplicates summary:** when `auditArchive` reports unrecorded files, have
  `findDuplicates` say "N files are not on the list yet; run repair first" — the finder is
  manifest-only and cannot see the force-quit orphan it was written for.
- Add a `SECURITY.md` row for `daily.log` (`~/Library/Logs/brightwheel-archive/` and the
  Linux/Windows equivalents): it holds the CLI's scrubbed stdout and Node's stderr, and
  therefore child names in progress lines; macOS diagnostic collection gathers
  `~/Library/Logs`. Consider a size cap later.
- `removeDuplicates` is sound as written (recomputes before deleting; only paths the fresh
  report lists as extras; sidecars go with the copy). Leave it.

## Part 2 — Main-side fixes, in priority order

Each item names the lane in QUESTIONS that argued it. Sizes are S unless marked.

**Now (correctness and privacy):**
1. **A2-1** Refuse non-cookie characters at the boundary. In `config.ts`
   `normaliseCookieInput` (both the prefixed and the bare form) and in `loadSession`,
   return null unless every character matches `/^[\x21\x23-\x2B\x2D-\x3A\x3C-\x5B\x5D-\x7E]+$/`
   (RFC 6265 cookie-octet: no CTL, whitespace, DQUOTE, comma, semicolon, backslash).
   Test: a pasted value with a line break gets the friendly 400 and the response body
   contains no byte of the pasted text; today it echoes everything after the newline.
   Also recognise a copied DevTools row (`_brightwheel_v2<TAB>value<TAB>…`) and refuse
   any value containing whitespace, and accept a bare value containing `=` when it has no
   `;` and no whitespace (A1-2).
2. **A2-2 / A2-3** One `readJson(req)` helper in `web/server.ts` that maps `SyntaxError`
   and "too large" to a fixed 400 text, used by `/api/session` and `/api/config` (and the
   scheduling routes after 1.4); check `typeof cookie === 'string'`. Scrub the server's
   progress stream and `result.warnings` before `lastResult` is served, as `cli.ts` does.
3. **B4-4** Clamp the incremental cut-off to the instant the walk began (`sync.ts`,
   where `newest` is advanced and written): one future-dated record currently ends every
   later run after three pages with no warning. Optionally warn (count only) when any post
   is more than a day in the future. Test on the mock: a future-dated post, then 35 new
   posts; all 35 must be saved.
4. **A4-1 / B3-2** `.gitleaks.toml`: make the signed-URL rule `(?i)` and add `~` to the
   value class; prefix the two allowlist regexes with `(?i)`. Add a test that loads every
   rule and allowlist regex from the TOML (a 15-line parser, no dependency) and proves a
   CloudFront-shaped `?Expires=…&Signature=…&Key-Pair-Id=…` URL and a `_brightwheel_v2=`
   cookie fire, and `test-session-value` does not.
5. **A4-2** `security.yml`: `gitleaks/gitleaks-action@v3` and `GITHUB_TOKEN: ${{
   secrets.GITHUB_TOKEN }}` in the step env. Add a `.gitleaksignore` for the one synthetic
   finding at d47782c (`fix-privacy.test.js`, signed-media-url) — the fingerprint format is
   `<sha>:<path>:<rule-id>:<line>`.
6. **B1-7** `verify.ts` `raw()`: use `schema.ts`'s `assertJsonResponse` with a label
   instead of the hand-rolled check, so a session that dies mid-verify prints a label, not
   `/guardians/<object_id>/students`. Reproduce with the mock's
   `expireSessionAfterRequests`.
7. **A1-1** `<textarea id="cookie">` gets `spellcheck="false" autocomplete="off"
   autocapitalize="off" autocorrect="off" data-1p-ignore data-lpignore="true"`, with a
   source-text test.
8. **A2-4** `login` must not echo the paste: mute the readline output while the question
   is pending (the standard password-prompt idiom), print the fingerprint after.
9. **Docs must tell the truth about what was checked.** `CLAUDE.md` Status: replace "The
   API surface is UNVERIFIED" with the B-preamble wording; 121 → 122 tests (and the win32
   line: 120 passed, 2 skipped); "CI runs the same suite" → "the CI matrix is written for …
   and has never run: there is no remote". `README.md` ~407–411 ("Nobody has yet run
   this against a real account") contradicts line ~370 — rewrite per the B preamble.
   `SECURITY.md`: the gitleaks row is a merge gate, not a fork control, and CI has never
   run; the `Secret` row should name the `#private` field, the three `.expose()` sites,
   that heap snapshots contain the plaintext, and that the raw string exists before
   wrapping (A2). `secrets.ts` comments: no `bug-report` command exists; `scrub` only
   recognises prefixed values. Wording: "one-time"/"per-run" token → "generated once per
   `setup` launch" (`server.ts` header, `GUIDE.md`, `SECURITY.md`); "a page opens" → "it
   prints a link to paste" (README). Delete the dead `openBrowser` option.
10. **C6-3 sweep of "taken".** README tag tables (`EXIF:DateTimeOriginal` row, the
    QuickTime row, "the clock in the room"), the "Save image" sentence (qualify: saved in
    one sitting; the website's files were checked, the app's were not), `GUIDE.md`'s tree
    line and the "wrong week" troubleshooting row, `verify.ts`'s "Upload time is not
    capture time" narrative, `schema.ts`'s `capturedAt` comment. Add three sentences to
    the GUIDE under "What you end up with" about dates. Add one README sentence (C5): plain
    `exiftool` shows a video's `CreateDate` as the UTC wall clock; photo apps show local.
    Never write "only" anywhere.
11. **README opening (C6-1).** Lead with independence from the account; keep the metadata
    sentence as one item. Draft in QUESTIONS C6; keep line 3 and the tree.

**Next:**
12. **B1-1/2/3/5, B4-1** Extend `verify` (counts and names only, never values): request
    page 1 and report how many of its ids appear on page 0; ask `page_size=100` (export
    `DEFAULT_PAGE_SIZE` from `client.ts`) and report the returned count and the envelope's
    `page_size`; count non-`ac_photo` records on the filtered page; list `media.*` and
    `video_info.*` key names and the HEAD's content-type and content-length rounded to
    100 KB; print the signature's remaining life as a duration via `signedUrlExpiry`;
    report whether `event_date` and `created_at` are each non-increasing down the page.
    Update the header comment and the "three API reads" count in `verify.ts`, README and
    QUESTIONS B1 together. Then the owner re-runs `verify` and pastes the report into
    QUESTIONS B1 under "Settled".
13. **B4-2 / B4-3** Parse `created_at` into `postedAt` on `MediaActivity` (nullable, falls
    back to `capturedAt`), write it into the sidecar and manifest provenance, and count a
    page as older only when every post is older by both timestamps; track `newest` as the
    max over both. Give the rule one named helper. Existing manifests need no migration.
    Give the mock a way to emit records where the two differ and to sort by either.
14. **A3-3 / A3-4** Accept `?token=` only on `GET /`; require `x-setup-token` on
    `/api/*`; drop the `?token=` suffix from the page's `api()` helper. Make the Origin
    check port-exact. Update the tests that put `?token=` on `/api/` URLs.
15. **C4-1** Keep `tagChildName: true`; replace `tagNote` with `tagContext: false`
    covering nursery (the location field), poster and note; effective only while names is
    on. Grey out the dependent switch in the page. Fix the README tag tables, the
    `archive.json` description, `SECURITY.md`'s "identified photographs" row and the GUIDE
    options table. Give the mock one caption that names the second child and a room.
    Regenerate `docs/images/03-options.png` once.
16. **C1** Fold `media-ferry` into `packages/brightwheel-archive/src/ferry/` (keep the
    barrel; fix three import sites and three relative-dist test imports; delete the
    fourteen unused exports; write `names.ts`'s control-character classes as escapes).
    Delete `dist/api/login.*` and add a `prepack` that removes `dist` before `tsc --build`.
    **Owner decision first:** publish to npm (then a README inside the package dir) or
    document the clone route and remove every `npx brightwheel-archive` line from README
    and GUIDE. Either way the SECURITY.md dependency row changes.
17. **B3-3/4/5** Add `hash-algorithm` to `SIGNATURE_PARAMS`; make the mock mint
    CloudFront-shaped URLs (`Expires` in seconds, `Signature`, `Key-Pair-Id`) and read them
    case-insensitively; reword the `sync.ts`/`url.ts` comments that call the unit a guess.
18. **A4-4/5** Ignore `session.json.tmp`, `config.json.tmp`, `archive.json.tmp`, `*.part`
    (mirror in `.dockerignore`, add cases to `verify-ignores.sh`). Then a zero-dependency
    `scripts/check-staged.mjs` pre-commit hook wired by `"prepare": "git config
    core.hooksPath .githooks"` that applies the TOML rules to `git show :<path>` of staged
    files; document that it cannot reach a parent editing on GitHub's web UI.
19. **C6-5** Rename the sidecar/manifest key `capturedAt` → `postedAt`, reading both;
    the only other reader is `maintenance.ts` after 1.4.

**Later:** A1-11 (print session age; learn the lifetime), A3-6 (one-shot bootstrap URL if
auto-open is ever wanted), A4-6 (narrow the PNG gitignore exception), B4-5 (express the
slack in posts), B4-7 (server-side `start_date` window, after a read-only probe), C4-6
(week README says what the files carry), C6-8 (say what Brightwheel itself offers),
C6-10 (package keywords), D (screenshot `--check`; Playwright smoke test in the
screenshots job; Windows cells watched green once a remote exists).

**Never (decided, do not reopen):** a cookie in place of the URL token; a bookmarklet or
console snippet; reading browser cookie stores; a bundled or CDP-driven browser;
perceptual dedupe; names off by default; anonymising folders and sidecars; masking the
paste box as a password field; a heuristic "long base64" scrub rule; gitleaks, husky,
lefthook or pre-commit as dependencies; probing URL variants the API did not hand over;
sending the session to the media host; page_size escalation past 100; a clock-skew
margin on expiry; decoding `Policy=`.

## Part 3 — Questions only the owner can answer

Record each answer in QUESTIONS-FOR-FABLE.md next to its entry.

1. Publish to npm, or clone-and-build? (C1, item 16.) Decides the README.
2. Product name: `Brightwheel Archive` everywhere, or a recorded rename everywhere? (1.1.)
3. Run the revocation test: paste a session, sign out of Brightwheel in the browser, run
   `doctor`. If the session still works, four documents must stop saying "immediately".
4. Read the `HttpOnly` tick for `_brightwheel_v2` in the DevTools table and record it (A1).
5. From the 2026-09-21 run's terminal, if it survives: how many items per page did
   "Looked through" report? (page size; B1/B4.)
6. After item 12 lands, re-run `verify` and paste the report into B1.
7. Is a second nursery's `verify` report obtainable (a friend's account)? (B2.)

## Repository hygiene

By the method in `CLAUDE.md` (`wc -l` over `packages/*/src/**/*.ts`, no tests, scripts or
`dist`): 5,640 on `main` at fcdd99e; 5,610 / 6,344 / 6,098 / 7,512 on shell / cookie-help /
native-dialogs / scheduling; about 8,730 with all four merged. The audit is due at
12,600, so not yet; `main` now carries the merged figure — update the CLAUDE.md line and
commit.
